const nameError = 'Field can only contain alphabetic characters'
const emailError = 'Must containt a valid email address (example@test.com)'
const passwordError = 'Password does not meet requirements'

export const errorMessage = {
    nameError,
    emailError,
    passwordError
}
